pipline {
   agent any
   stages {
      stage('build') {
         steps {
            echo "hi build phase"
         }
      }

      stage('test') {
         steps {
            echo "hi test phase"
         }
      }

      stage('deploy') {
         steps {
            echo "hi deploy phase"
         }
      }

   }
}